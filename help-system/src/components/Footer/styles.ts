import styled from "styled-components";

export const Footer = styled.footer`
  background: rgba(17, 24, 39, 0.96);
  color: white;
  text-align: center;
  padding: 1.5rem;
  margin-top: 1.25rem;

  p {
    opacity: 0.85;
    margin: 0;
    font-size: 0.95rem;
  }
`;
